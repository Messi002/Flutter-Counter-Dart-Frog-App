/// A Dart package which manages the counter domain.
library counter_repository;

//Coming from the package on pub.dev
export 'package:web_socket_client/web_socket_client.dart'
    show
        ConnectionState,
        Connecting,
        Connected,
        Disconnecting,
        Disconnected,
        Reconnecting,
        Reconnected;

export 'src/counter_repository.dart';
